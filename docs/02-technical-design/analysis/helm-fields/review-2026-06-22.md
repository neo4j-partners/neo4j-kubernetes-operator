# Design review — 2026-06-22

**Reviewer**: design-consistency-reviewer (Phase 6)  
**Scope**: helm-fields pipeline Phases 3–6 vs `09-crd-spec/neo4j/`, BDRs, FRs

---

## Pass / fail

| Gate | Status |
|------|--------|
| G-SC (semantic concerns) | **PASS** |
| G3 Aggregation | **PASS** |
| G4 Versioning / breaking register | **PASS** (6 scheduling/image rows reclassified `safe`) |
| G-FR-1 FR coverage | **PASS** |
| G6 Spec terminology alignment | **PASS** (after `crd_target` rename) |
| G6 BDR index vs disk | **PASS** |
| Ready for `11-helm-mapping.md` export | **yes** (with open BDR decisions noted) |

**Overall**: **PASS with conditions** — 4 BDRs remain `proposed`; BDR-006 not drafted; BDR-009 may amend BDR-002.

---

## Conflicts found (resolved or tracked)

| # | Location | Issue | Resolution |
|---|----------|-------|------------|
| 1 | `aggregation-matrix.md` (was) | BDR-005 labeled TLS, BDR-006 storage | Fixed — 005=storage, 006=TLS queue |
| 2 | `fields/*.md` + `_index.csv` | `spec.storage` / `spec.networking` vs `spec.md` | **Fixed** — renamed to `persistence` / `connectivity` |
| 3 | BDR-002 vs BDR-009 | Single STS vs per-pool STS | Tracked — BDR-009 recommends Option B (amend BDR-002) |
| 4 | `readme.md` BDR table | Out-of-order rows | Fixed — numeric order |
| 5 | `_index.csv` | `neo4j.labels` missing | Logged in `validation-gaps.md` VAL inventory |

---

## Terminology checklist

- [x] `primaries` / `secondaries.analytics` / `secondaries.read` — consistent in spec, BDR-002, rules
- [x] No `secondaries[]` free `name` list
- [x] No `serverRole` field
- [x] `pluginDefinitions` Option E — BDR-004 + spec aligned
- [x] `connectivity` not `networking` in spec + crd_target
- [x] `persistence` not `storage` in spec + crd_target

---

## Traceability checklist

- [x] 28 `breaking` rows — all have `bdr_id` or BC-006 register entry
- [x] All `AGG-*` groups ≥ 2 paths
- [x] 93 field docs with `client_need`
- [x] BDR-005, 007, 008, 009 files exist on disk
- [ ] BDR-002, BDR-004 still `proposed` — not accepted

---

## Recommended fixes (ordered)

1. **User decision**: Accept or amend BDR-002, BDR-004, BDR-005, BDR-007, BDR-008, BDR-009.
2. **If BDR-009 Option B accepted**: Amend BDR-002 "one STS" wording + `spec.md` + `validation.md` for per-pool STS.
3. ~~**Draft BDR-006** before V1 OpenAPI lock if `spec.trust` BYO contract is finalized.~~ **Done** — [BDR-006](../../decision-records/business/006-tls-trust-model.md) proposed.
4. **Inventory refresh**: Add `neo4j.labels` to `_index.csv`.
5. **Export**: Generate `design/11-helm-mapping.md` from `_index.csv` columns.
6. **CEL pass**: Implement VAL-001…007 in `validation.md` as BDRs land.

---

## Open decisions (not blockers for mapping export)

| BDR | Question |
|-----|----------|
| BDR-005 | Option C curated `mode` enum — confirm |
| BDR-007 | Option B intent-based connectivity — confirm |
| BDR-008 | Option C hybrid config — confirm |
| BDR-009 | Option B per-pool STS vs C tail-only — **blocks scale UX** |

---

## Ready for `11-helm-mapping.md` export: **yes**

Prerequisite: user acknowledges proposed BDRs are not yet accepted.
