apiVersion: v1
kind: Service
metadata:
  name: webhook-service
  namespace: neo4j-operator-system
  labels:
    control-plane: controller-manager
spec:
  ports:
    - port: 443
      targetPort: 9443
      protocol: TCP
      name: webhook
  selector:
    control-plane: controller-manager
