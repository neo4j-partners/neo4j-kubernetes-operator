1. CRD (required — large OpenAPI schema needs server-side apply):

   From a release asset:
   kubectl apply --server-side --force-conflicts \
     -f https://github.com/neo4j-partners/neo4j-kubernetes-operator/releases/download/v{{ .Chart.AppVersion }}/neo4j-crd-{{ .Chart.AppVersion }}.yaml

   From a local checkout:
   kubectl apply --server-side --force-conflicts -f config/crd/bases/neo4j.com_neo4js.yaml   # or: make install

2. Verify the controller:

   kubectl get deployment neo4j-operator-controller-manager -n {{ .Release.Namespace }}
   kubectl wait --for=condition=Available deployment/neo4j-operator-controller-manager \
     -n {{ .Release.Namespace }} --timeout=120s

3. Watch namespaces (WATCH_NAMESPACE): {{ include "neo4j-operator.watchNamespacesCSV" . }}
   The operator namespace {{ .Release.Namespace }} is not watched (NEO-016). Apply Neo4j CRs in a listed workload namespace.
{{- if .Values.metrics.enabled }}

   Operator metrics: HTTPS :{{ .Values.metrics.port }} with Kubernetes auth (NEO-017).
   Bind your Prometheus ServiceAccount to ClusterRole {{ include "neo4j-operator.fullname" . }}-metrics-reader.
{{- end }}

4. Apply a Neo4j CR in a watched namespace, e.g. examples/standalone/01-minimal.yaml

{{- if .Values.crd.installHint }}

Note: this chart does not embed the Neo4j CRD (annotation size / upgrade). Always install or upgrade the CRD with server-side apply before relying on new API fields.
{{- end }}
