# Purpose: Companion Opaque Secret for 10-secret-mounts.yaml / 11-full.yaml.
# Prerequisites: none — this Secret is itself a prerequisite for those examples.
# Apply: kubectl apply -f examples/storage/10-secret-mounts-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: dev-storage-secret-creds
type: Opaque
stringData:
  config.txt: |
    # lab placeholder — replace with real credentials/config before production use
    example=true
