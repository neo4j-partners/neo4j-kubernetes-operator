# Purpose: PodDisruptionBudget spanning all cluster pods (NEO-2-008).
# Prerequisites: none.
# Apply: kubectl apply -f examples/cluster/15-pdb.yaml
# Related README: ../README.md#cluster
#
# Creates policy/v1 PodDisruptionBudget "prod-pdb" selecting all pods for this CR
# (all pools). Default minAvailable is 2 when Cluster has ≥3 members.
# minAvailable must stay < total members (and not 100%) or admission/reconcile
# rejects it — otherwise node drains block forever (ADD-03).
# Verify: kubectl get pdb prod-pdb -o yaml
apiVersion: neo4j.com/v1beta1
kind: Neo4j
metadata:
  name: prod-pdb
spec:
  edition: enterprise
  version: "2026.05.0"
  license:
    accept: "yes"
  topology:
    mode: Cluster
    primaries:
      members: 3
  storage:
    volumes:
      data:
        mode: Dynamic
        dynamic:
          size: 10Gi
  auth:
    generatePassword: true
  trust:
    # NEO-004: explicit opt-in to plaintext operator admin Bolt (Warning event).
    # Prefer trust.certificates.bolt for verified TLS instead.
    enabled: false
    insecureAdminConnection: true
  podDisruptionBudget:
    enabled: true
    # minAvailable: 2   # optional override (int or percentage string)
