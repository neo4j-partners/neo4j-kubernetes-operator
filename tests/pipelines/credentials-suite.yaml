setup:
  - operator/install

teardown:
  - cleanup/operator

# ensure-secret runs before deploy so passwordSecretRef resolves on first reconcile.
case_run:
  - credentials/ensure-secret
  - deploy/standalone

case_teardown:
  - cleanup/standalone
  - credentials/cleanup-secret
