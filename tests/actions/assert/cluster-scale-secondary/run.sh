#!/usr/bin/env bash
# assert/cluster-scale-secondary — pure assertion; verify.sh patches
# topology.secondaries.<pool>.members on the already-deployed CR, twice, and observes the
# scale-out and the drained scale-in.
set -euo pipefail
true
