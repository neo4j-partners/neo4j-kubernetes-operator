#!/usr/bin/env bash
# assert/storage-immutable — pure assertion; verify.sh attempts each forbidden storage transition
# on the already-deployed CR and checks the API server refuses it.
set -euo pipefail
true
