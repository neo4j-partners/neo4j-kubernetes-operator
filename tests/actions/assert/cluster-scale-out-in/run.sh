#!/usr/bin/env bash
# assert/cluster-scale-out-in — pure assertion; verify.sh patches topology.primaries.members
# on the already-deployed CR, twice, and observes the scale-out and the drained scale-in.
set -euo pipefail
true
