#!/usr/bin/env bash
# Happy path — fixed reconciliation (one classic case per layer).
# Operator case is chosen from cloud in reconcile.sh after cloud load.

export E2E_PROFILE=happy-path
export NEO4J_CASE=standalone-minimal
