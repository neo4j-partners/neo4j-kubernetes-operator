// Package validation holds validating webhook stubs for rules delegated from CEL (ADR-001).
package validation

// Neo4jValidator will implement webhook-only validation rules from validation.md.
// Slice 1 relies on CRD CEL admission; webhook implementation is deferred.
