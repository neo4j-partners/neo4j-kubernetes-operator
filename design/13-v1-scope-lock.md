# V1 scope lock

Frozen list of functional requirements, acceptance criteria, and tests required for the V1 release.

**Derived from**: `02-acceptance_criteria_library.csv` + `04-test_catalog.csv` (filter `V1=Yes`, `Priority=P0`).

**Status**: `[ ]` to do — generated after P0 spec (`09`–`12`) is locked.

**Former name**: `13-dod-v1.md` (renamed — “definition of done” was ambiguous; this file is the **V1 commitment scope**, not a generic DoD checklist).

---

## Purpose

- Single source of truth for **what V1 includes and excludes**
- Input to `17-roadmap.md` milestones and release date commitment
- Gate for `EST-TST-090` (V1 release validation) and `EST-CON-007` (scope lock)

*(Content to be generated — AC list, test IDs, explicit non-goals.)*
